import React from "react";
import { Button } from "../../ui/button";

const CreateAccountButton = () => {
  return <Button variant="createAccount">Create for free</Button>;
};

export default CreateAccountButton;
